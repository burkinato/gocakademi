# ==============================================
# DATABASE CONFIGURATION
# ==============================================
# PostgreSQL Database Settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gocakademi
DB_USER=postgres
DB_PASSWORD=CBIsil123$$

# ==============================================
# SERVER CONFIGURATION
# ==============================================
PORT=3001
NODE_ENV=development

# ==============================================
# JWT CONFIGURATION
# ==============================================
# Main JWT secret for access tokens
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_generate_with_crypto

# JWT refresh token secret
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here_generate_with_crypto

# Token expiration times
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# ==============================================
# FRONTEND CONFIGURATION
# ==============================================
# Frontend URL for CORS
CLIENT_URL=http://localhost:5173

# Backend API URL for frontend (NO /api suffix)
VITE_API_URL=http://localhost:3001

# ==============================================
# ADMIN CONFIGURATION
# ==============================================
# Default admin credentials (CHANGE IN PRODUCTION!)
ADMIN_EMAIL=admin@gocakademi.com
ADMIN_PASSWORD=Admin123!@#
